print = __builtin_print;

infix(SUM) + = (a, b) => __builtin_add(a, b);

func = (a, b) => 4;

Human = class {
    name: String = "";
    age: Int = 0;
};


x: Human = Human("Ali", 22);

print(Human);

